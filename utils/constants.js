// Define constants used in the application

const DEFAULT_SHIPPING_COST = 10.0;
const MAX_SHIPPING_WEIGHT = 10;
const MAX_QUANTITY_PER_ORDER = 100;
const CURRENCY = 'USD';

module.exports = {
  DEFAULT_SHIPPING_COST,
  MAX_SHIPPING_WEIGHT,
  MAX_QUANTITY_PER_ORDER,
  CURRENCY,
};
